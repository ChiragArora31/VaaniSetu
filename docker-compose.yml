services:
  web:
    build: .
    ports:
      - "8501:8501"
    environment:
      BAIF_MODEL_PROFILE: "quality"
      BAIF_ALLOW_MODEL_DOWNLOAD: "0"
    volumes:
      - ./models:/app/models
      - ./outputs:/app/outputs
      - ./temp:/app/temp
